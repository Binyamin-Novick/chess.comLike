package chessengine.advance.multyplayer;
// this class keeps track if players are still active

public class playerActiveChecker {
    //will run on its own thread will keep track of time  since plaer been active if more than the limit will force that side to forfite
    //it does this by messgeing the member mager asking is player logged on
    public playerActiveChecker(MultyPlayerEngine multyPlayerEngine){}



}
