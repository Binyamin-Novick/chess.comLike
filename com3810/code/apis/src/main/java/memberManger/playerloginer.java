package memberManger;

public class playerloginer {
    public playerloginer(MemberManger memberManger){}
    public String validLoggin(String user,String password){}
    // will messgege member datat base and check if its a good log in
    //if not reurn null else will return user data like elo
}
