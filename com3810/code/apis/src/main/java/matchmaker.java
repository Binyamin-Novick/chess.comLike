public class matchmaker {
    /*
    will have a thread runing that will listen
    for a messge from the user they would like to join will then make re request to membor manger for the requered data
    for member data need for match makeing algerthym
    will then add the user to the poll the match maker is makeing matches from
    when match maker gets a match in will messege the game engine of the match and the relevent data  it will then remove the matching mebers from the matchpool


     */
    public matchmaker(){}
}
